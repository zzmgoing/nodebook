on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@master
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        server: byu4287180001.my3w.com/htdocs/books/node
        username: byu4287180001
        password: ${{ secrets.android_book_ftp_pwd_zzm }}
        ignore: .gitignore
